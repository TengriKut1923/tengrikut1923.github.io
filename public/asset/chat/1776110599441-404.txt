---
import { Core } from '@/layout';
import { SITE_META, CONST_DATA } from '@/setup';

export const prerender = true;
---

<Core title={SITE_META.TITLE} noIndex={true}>
  <div id={CONST_DATA.CORE.STRINGS.STATUS_ERROR} hidden></div>
</Core>

<script>
  import { Status } from '@/nav';
  import { NAV_DATA, CONST_DATA } from '@/setup';

  if (document.getElementById(CONST_DATA.CORE.STRINGS.STATUS_ERROR)) {
    Status.gotoPath(NAV_DATA.ROUTE.HOME);
  }
</script>